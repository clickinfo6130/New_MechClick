<Window x:Class="ExcelToPostgres.DbSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExcelToPostgres"
        mc:Ignorable="d"
        Title="PostgreSQL 연결 설정" 
        Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <TextBlock Text="🐘 PostgreSQL 연결 설정" FontSize="18" FontWeight="Bold" 
                   Foreground="#37474F" Margin="0,0,0,20"/>

        <!-- 설정 폼 -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="호스트:" VerticalAlignment="Center" Grid.Row="0"/>
            <TextBox x:Name="TxtHost" Grid.Row="0" Grid.Column="1" Margin="0,5" Padding="8,6"/>

            <TextBlock Text="포트:" VerticalAlignment="Center" Grid.Row="1"/>
            <TextBox x:Name="TxtPort" Grid.Row="1" Grid.Column="1" Margin="0,5" Padding="8,6"/>

            <TextBlock Text="데이터베이스:" VerticalAlignment="Center" Grid.Row="2"/>
            <TextBox x:Name="TxtDatabase" Grid.Row="2" Grid.Column="1" Margin="0,5" Padding="8,6"/>

            <TextBlock Text="사용자명:" VerticalAlignment="Center" Grid.Row="3"/>
            <TextBox x:Name="TxtUsername" Grid.Row="3" Grid.Column="1" Margin="0,5" Padding="8,6"/>

            <TextBlock Text="비밀번호:" VerticalAlignment="Center" Grid.Row="4"/>
            <PasswordBox x:Name="TxtPassword" Grid.Row="4" Grid.Column="1" Margin="0,5" Padding="8,6"/>
        </Grid>

        <!-- 버튼 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="취소" Width="80" Click="BtnCancel_Click" Background="#9E9E9E"/>
            <Button Content="저장" Width="80" Click="BtnSave_Click" Background="#4CAF50"/>
        </StackPanel>
    </Grid>
</Window>
